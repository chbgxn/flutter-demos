import 'package:flutter/material.dart';

class TakeoutScreen extends StatelessWidget {
  const TakeoutScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('TakeoutScreen: please wait a minute'),
      ),
    );
  }
}